# Find the root of a git repository

Returns the path of the current git repository

## Usage

``` r
find_git_root(path = ".")
```

## Arguments

- path:

  option string giving the path to use a starting point (i.e. that is
  part of the repository)
