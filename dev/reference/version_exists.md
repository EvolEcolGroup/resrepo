# Check that a version exists in versions

Check that a version exists in versions

## Usage

``` r
version_exists(version, quiet = TRUE)
```

## Arguments

- version:

  The version to check

- quiet:

  Should the function be quiet?

## Value

TRUE if the version exists, FALSE otherwise
